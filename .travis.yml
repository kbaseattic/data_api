language: python
python:
  - "2.7"
  - "3.4"

install:
  - cd lib
  - pip install coveralls
  - pip install -r requirements.txt
  - cd ..

script:
  - cd lib
  - touch .coverage
  - nosetests --with-coverage --cover-package=data_api
  - cd ..

after_success:
  - cd lib
  - coveralls
  - cd ..
