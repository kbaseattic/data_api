"""
KBase Data API package.
"""

import os
#from doekbase.data_api.nav import DBConnection, Finder

#def browse(workspace=None, **kw):
#    """Create and return a `.nav.Finder` instance, which allows
#    the user to easily list and retrieve objects in a given
#    workspace through the Data API.
#
#    Args:
#      workspace: Integer identifier for the workspace
#
#    Returns:
#      An initialized Finder instance.
#    """
#    conn = DBConnection(workspace=workspace, **kw)
#    return Finder(conn)

        
