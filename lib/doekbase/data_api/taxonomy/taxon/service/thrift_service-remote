#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.9.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py:new_style
#

import sys
import pprint
from urlparse import urlparse
from thrift.transport import TTransport
from thrift.transport import TSocket
from thrift.transport import THttpClient
from thrift.server import THttpServer
from thrift.protocol import TBinaryProtocol

import thrift_service
from ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
  print ''
  print 'Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] function [arg1 [arg2...]]'
  print ''
  print 'Functions:'
  print '  ObjectReference get_parent(string token, ObjectReference ref)'
  print '   get_children(string token, ObjectReference ref)'
  print '   get_genome_annotations(string token, ObjectReference ref)'
  print '  string get_scientific_lineage(string token, ObjectReference ref)'
  print '  string get_scientific_name(string token, ObjectReference ref)'
  print '  i32 get_taxonomic_id(string token, ObjectReference ref)'
  print '  string get_kingdom(string token, ObjectReference ref)'
  print '  string get_domain(string token, ObjectReference ref)'
  print '  byte get_genetic_code(string token, ObjectReference ref)'
  print '   get_aliases(string token, ObjectReference ref)'
  print ''
  sys.exit(0)

pp = pprint.PrettyPrinter(indent = 2)
host = 'localhost'
port = 9090
uri = ''
framed = False
http = False
argi = 1

if sys.argv[argi] == '-h':
  parts = sys.argv[argi+1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  argi += 2

if sys.argv[argi] == '-u':
  url = urlparse(sys.argv[argi+1])
  parts = url[1].split(':')
  host = parts[0]
  if len(parts) > 1:
    port = int(parts[1])
  else:
    port = 80
  uri = url[2]
  if url[4]:
    uri += '?%s' % url[4]
  http = True
  argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
  framed = True
  argi += 1

cmd = sys.argv[argi]
args = sys.argv[argi+1:]

if http:
  transport = THttpClient.THttpClient(host, port, uri)
else:
  socket = TSocket.TSocket(host, port)
  if framed:
    transport = TTransport.TFramedTransport(socket)
  else:
    transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol.TBinaryProtocol(transport)
client = thrift_service.Client(protocol)
transport.open()

if cmd == 'get_parent':
  if len(args) != 2:
    print 'get_parent requires 2 args'
    sys.exit(1)
  pp.pprint(client.get_parent(args[0],eval(args[1]),))

elif cmd == 'get_children':
  if len(args) != 2:
    print 'get_children requires 2 args'
    sys.exit(1)
  pp.pprint(client.get_children(args[0],eval(args[1]),))

elif cmd == 'get_genome_annotations':
  if len(args) != 2:
    print 'get_genome_annotations requires 2 args'
    sys.exit(1)
  pp.pprint(client.get_genome_annotations(args[0],eval(args[1]),))

elif cmd == 'get_scientific_lineage':
  if len(args) != 2:
    print 'get_scientific_lineage requires 2 args'
    sys.exit(1)
  pp.pprint(client.get_scientific_lineage(args[0],eval(args[1]),))

elif cmd == 'get_scientific_name':
  if len(args) != 2:
    print 'get_scientific_name requires 2 args'
    sys.exit(1)
  pp.pprint(client.get_scientific_name(args[0],eval(args[1]),))

elif cmd == 'get_taxonomic_id':
  if len(args) != 2:
    print 'get_taxonomic_id requires 2 args'
    sys.exit(1)
  pp.pprint(client.get_taxonomic_id(args[0],eval(args[1]),))

elif cmd == 'get_kingdom':
  if len(args) != 2:
    print 'get_kingdom requires 2 args'
    sys.exit(1)
  pp.pprint(client.get_kingdom(args[0],eval(args[1]),))

elif cmd == 'get_domain':
  if len(args) != 2:
    print 'get_domain requires 2 args'
    sys.exit(1)
  pp.pprint(client.get_domain(args[0],eval(args[1]),))

elif cmd == 'get_genetic_code':
  if len(args) != 2:
    print 'get_genetic_code requires 2 args'
    sys.exit(1)
  pp.pprint(client.get_genetic_code(args[0],eval(args[1]),))

elif cmd == 'get_aliases':
  if len(args) != 2:
    print 'get_aliases requires 2 args'
    sys.exit(1)
  pp.pprint(client.get_aliases(args[0],eval(args[1]),))

else:
  print 'Unrecognized method %s' % cmd
  sys.exit(1)

transport.close()
